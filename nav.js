(()=>{const nav=document.querySelector('.nav');if(!nav)return;const groups=[...nav.querySelectorAll('details')];groups.forEach(d=>d.addEventListener('toggle',()=>{if(d.open)groups.filter(x=>x!==d).forEach(x=>x.open=false)}));document.addEventListener('click',e=>{if(!nav.contains(e.target))groups.forEach(d=>d.open=false)});document.addEventListener('keydown',e=>{if(e.key==='Escape'){const active=groups.find(d=>d.open);groups.forEach(d=>d.open=false);if(active)active.querySelector('summary').focus();nav.classList.remove('open');document.body.classList.remove('drawer-open');document.querySelector('.menu')?.setAttribute('aria-expanded','false')}});groups.forEach(d=>{if(d.querySelector('a.active'))d.classList.add('current')})})();